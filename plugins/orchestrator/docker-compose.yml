version: '2'
services:
  rethinkdb:
    image: rethinkdb:2.3.5
    restart: always
  server:
    image: equate/mvp:latest
    restart: always
    command: "mvp serve --store=rethinkdb://admin@rethinkdb:28015"
    ports:
     - "${HTTP_PORT}:8000"
